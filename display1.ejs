<!DOCTYPE html>
<html>
<head>
    <title>Form Data</title>
</head>
<body style="background-color: #121212; color: white; text-align: center; font-family: Arial, sans-serif; padding: 20px;">
    <h1 style="color: cyan; text-shadow: 0px 0px 10px cyan;">Submitted Data</h1>
    
    <table border="1" style="width: 80%; margin: auto; border-collapse: collapse; box-shadow: 0 0 10px rgba(0, 255, 255, 0.5);">
        <tr style="background: rgba(0, 255, 255, 0.2);">
            <th>Name</th>
            <th>Mobile Number</th>
            <th>Email</th>
            <th>Password</th>
        </tr>
        <% if (Array.isArray(formData)) { %>
            <% formData.forEach(data => { %>
                <tr style="background: rgba(0, 255, 255, 0.1);">
                    <td><%= data.name %></td>
                    <td><%= data.mobile %></td>
                    <td><%= data.email %></td>
                    <td><%= data.password %></td>
                </tr>
            <% }) %>
        <% } else { %>
            <tr style="background: rgba(0, 255, 255, 0.1);">
                <td><%= formData.name %></td>
                <td><%= formData.mobile %></td>
                <td><%= formData.email %></td>
                <td><%= formData.password %></td>
            </tr>
        <% } %>
    </table>

    <br>
    <a href="/" style="padding: 10px 20px; background-color: cyan; color: black; border-radius: 5px; text-decoration: none;">Submit Another Entry</a>
</body>
</html>
